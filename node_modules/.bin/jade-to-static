#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jade-to-static/jade-to-static.js" "$@"
  ret=$?
else 
  node  "$basedir/../jade-to-static/jade-to-static.js" "$@"
  ret=$?
fi
exit $ret
